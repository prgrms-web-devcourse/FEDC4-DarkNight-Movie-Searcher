import styled from '@emotion/styled';
import { palette } from './stylesConstants';

export const Highlight = styled.p`
  font-size: 20px;
  font-weight: bold;
  color: ${palette.themeColor};
  padding: 4px;
`;

export const Text = styled.p`
  color: ${palette.themeColor};
`;
